#include "MAIN.H"
#include <stdio.h>

/**
 * main - check the code for ALX Students
 *
 * Return: Always 0.
 */
int main(void)
{
	 char *str;
	 int len;

	 str = "MAIN!";
	 len = _strlen(str);
	 printf("%d\n", len);
	 return (0);
}
